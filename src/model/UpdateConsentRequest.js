/**
 * Infrahub-API
 * Leverage the Infrahub API and Hyperstack platform to easily create, manage, and scale powerful GPU virtual machines and their associated resources.   Access this SDK to automate the deployment of your workloads and streamline your infrastructure management.  To contribute, please raise an issue with a bug report, feature request, feedback, or general inquiry.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdateConsentRequest model module.
 * @module model/UpdateConsentRequest
 * @version v1.52.3-alpha
 */
class UpdateConsentRequest {
    /**
     * Constructs a new <code>UpdateConsentRequest</code>.
     * @alias module:model/UpdateConsentRequest
     * @param action {module:model/UpdateConsentRequest.ActionEnum} 
     */
    constructor(action) { 
        
        UpdateConsentRequest.initialize(this, action);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, action) { 
        obj['action'] = action;
        obj['consent_method'] = 'web_checkbox';
    }

    /**
     * Constructs a <code>UpdateConsentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateConsentRequest} obj Optional instance to populate.
     * @return {module:model/UpdateConsentRequest} The populated <code>UpdateConsentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateConsentRequest();

            if (data.hasOwnProperty('action')) {
                obj['action'] = ApiClient.convertToType(data['action'], 'String');
            }
            if (data.hasOwnProperty('consent_method')) {
                obj['consent_method'] = ApiClient.convertToType(data['consent_method'], 'String');
            }
            if (data.hasOwnProperty('consent_version')) {
                obj['consent_version'] = ApiClient.convertToType(data['consent_version'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateConsentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateConsentRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateConsentRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['action'] && !(typeof data['action'] === 'string' || data['action'] instanceof String)) {
            throw new Error("Expected the field `action` to be a primitive type in the JSON string but got " + data['action']);
        }
        // ensure the json data is a string
        if (data['consent_method'] && !(typeof data['consent_method'] === 'string' || data['consent_method'] instanceof String)) {
            throw new Error("Expected the field `consent_method` to be a primitive type in the JSON string but got " + data['consent_method']);
        }
        // ensure the json data is a string
        if (data['consent_version'] && !(typeof data['consent_version'] === 'string' || data['consent_version'] instanceof String)) {
            throw new Error("Expected the field `consent_version` to be a primitive type in the JSON string but got " + data['consent_version']);
        }

        return true;
    }


}

UpdateConsentRequest.RequiredProperties = ["action"];

/**
 * @member {module:model/UpdateConsentRequest.ActionEnum} action
 */
UpdateConsentRequest.prototype['action'] = undefined;

/**
 * @member {module:model/UpdateConsentRequest.ConsentMethodEnum} consent_method
 * @default 'web_checkbox'
 */
UpdateConsentRequest.prototype['consent_method'] = 'web_checkbox';

/**
 * Version identifier. Defaults to the current version for the consent type
 * @member {String} consent_version
 */
UpdateConsentRequest.prototype['consent_version'] = undefined;

/**
 * Consent-type-specific metadata
 * @member {Object} metadata
 */
UpdateConsentRequest.prototype['metadata'] = undefined;





/**
 * Allowed values for the <code>action</code> property.
 * @enum {String}
 * @readonly
 */
UpdateConsentRequest['ActionEnum'] = {

    /**
     * value: "granted"
     * @const
     */
    "granted": "granted",

    /**
     * value: "revoked"
     * @const
     */
    "revoked": "revoked"
};


/**
 * Allowed values for the <code>consent_method</code> property.
 * @enum {String}
 * @readonly
 */
UpdateConsentRequest['ConsentMethodEnum'] = {

    /**
     * value: "web_checkbox"
     * @const
     */
    "web_checkbox": "web_checkbox",

    /**
     * value: "api"
     * @const
     */
    "api": "api",

    /**
     * value: "admin_override"
     * @const
     */
    "admin_override": "admin_override"
};



export default UpdateConsentRequest;

