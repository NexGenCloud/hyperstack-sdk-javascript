/**
 * Infrahub-API
 * Leverage the Infrahub API and Hyperstack platform to easily create, manage, and scale powerful GPU virtual machines and their associated resources.   Access this SDK to automate the deployment of your workloads and streamline your infrastructure management.  To contribute, please raise an issue with a bug report, feature request, feedback, or general inquiry.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CompatibleImage from './CompatibleImage';

/**
 * The ImageRestrictions model module.
 * @module model/ImageRestrictions
 * @version v1.55.7-alpha
 */
class ImageRestrictions {
    /**
     * Constructs a new <code>ImageRestrictions</code>.
     * @alias module:model/ImageRestrictions
     */
    constructor() { 
        
        ImageRestrictions.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ImageRestrictions</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImageRestrictions} obj Optional instance to populate.
     * @return {module:model/ImageRestrictions} The populated <code>ImageRestrictions</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImageRestrictions();

            if (data.hasOwnProperty('compatible_images')) {
                obj['compatible_images'] = ApiClient.convertToType(data['compatible_images'], [CompatibleImage]);
            }
            if (data.hasOwnProperty('has_image_restrictions')) {
                obj['has_image_restrictions'] = ApiClient.convertToType(data['has_image_restrictions'], 'Boolean');
            }
            if (data.hasOwnProperty('restriction_type')) {
                obj['restriction_type'] = ApiClient.convertToType(data['restriction_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImageRestrictions</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImageRestrictions</code>.
     */
    static validateJSON(data) {
        if (data['compatible_images']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['compatible_images'])) {
                throw new Error("Expected the field `compatible_images` to be an array in the JSON data but got " + data['compatible_images']);
            }
            // validate the optional field `compatible_images` (array)
            for (const item of data['compatible_images']) {
                CompatibleImage.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['restriction_type'] && !(typeof data['restriction_type'] === 'string' || data['restriction_type'] instanceof String)) {
            throw new Error("Expected the field `restriction_type` to be a primitive type in the JSON string but got " + data['restriction_type']);
        }

        return true;
    }


}



/**
 * List of images this flavor is allowed to launch, with link metadata
 * @member {Array.<module:model/CompatibleImage>} compatible_images
 */
ImageRestrictions.prototype['compatible_images'] = undefined;

/**
 * Whether the flavor is restricted to a set of images
 * @member {Boolean} has_image_restrictions
 */
ImageRestrictions.prototype['has_image_restrictions'] = undefined;

/**
 * Either 'hard', 'soft', or null if no restrictions
 * @member {String} restriction_type
 */
ImageRestrictions.prototype['restriction_type'] = undefined;






export default ImageRestrictions;

