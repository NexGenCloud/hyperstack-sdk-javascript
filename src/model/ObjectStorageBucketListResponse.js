/**
 * Infrahub-API
 * Leverage the Infrahub API and Hyperstack platform to easily create, manage, and scale powerful GPU virtual machines and their associated resources.   Access this SDK to automate the deployment of your workloads and streamline your infrastructure management.  To contribute, please raise an issue with a bug report, feature request, feedback, or general inquiry.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ObjectStorageBucketResponse from './ObjectStorageBucketResponse';

/**
 * The ObjectStorageBucketListResponse model module.
 * @module model/ObjectStorageBucketListResponse
 * @version v1.46.4-alpha
 */
class ObjectStorageBucketListResponse {
    /**
     * Constructs a new <code>ObjectStorageBucketListResponse</code>.
     * @alias module:model/ObjectStorageBucketListResponse
     * @param buckets {Array.<module:model/ObjectStorageBucketResponse>} 
     */
    constructor(buckets) { 
        
        ObjectStorageBucketListResponse.initialize(this, buckets);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, buckets) { 
        obj['buckets'] = buckets;
    }

    /**
     * Constructs a <code>ObjectStorageBucketListResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ObjectStorageBucketListResponse} obj Optional instance to populate.
     * @return {module:model/ObjectStorageBucketListResponse} The populated <code>ObjectStorageBucketListResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ObjectStorageBucketListResponse();

            if (data.hasOwnProperty('buckets')) {
                obj['buckets'] = ApiClient.convertToType(data['buckets'], [ObjectStorageBucketResponse]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ObjectStorageBucketListResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ObjectStorageBucketListResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ObjectStorageBucketListResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['buckets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['buckets'])) {
                throw new Error("Expected the field `buckets` to be an array in the JSON data but got " + data['buckets']);
            }
            // validate the optional field `buckets` (array)
            for (const item of data['buckets']) {
                ObjectStorageBucketResponse.validateJSON(item);
            };
        }

        return true;
    }


}

ObjectStorageBucketListResponse.RequiredProperties = ["buckets"];

/**
 * @member {Array.<module:model/ObjectStorageBucketResponse>} buckets
 */
ObjectStorageBucketListResponse.prototype['buckets'] = undefined;






export default ObjectStorageBucketListResponse;

