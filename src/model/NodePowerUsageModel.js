/**
 * Infrahub-API
 * Leverage the Infrahub API and Hyperstack platform to easily create, manage, and scale powerful GPU virtual machines and their associated resources.   Access this SDK to automate the deployment of your workloads and streamline your infrastructure management.  To contribute, please raise an issue with a bug report, feature request, feedback, or general inquiry.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NodeStocksPayload from './NodeStocksPayload';
import PowerUsageModel from './PowerUsageModel';

/**
 * The NodePowerUsageModel model module.
 * @module model/NodePowerUsageModel
 * @version v1.25.0-alpha
 */
class NodePowerUsageModel {
    /**
     * Constructs a new <code>NodePowerUsageModel</code>.
     * @alias module:model/NodePowerUsageModel
     * @param openstackId {String} 
     */
    constructor(openstackId) { 
        
        NodePowerUsageModel.initialize(this, openstackId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, openstackId) { 
        obj['openstack_id'] = openstackId;
    }

    /**
     * Constructs a <code>NodePowerUsageModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NodePowerUsageModel} obj Optional instance to populate.
     * @return {module:model/NodePowerUsageModel} The populated <code>NodePowerUsageModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NodePowerUsageModel();

            if (data.hasOwnProperty('flavors')) {
                obj['flavors'] = ApiClient.convertToType(data['flavors'], ['String']);
            }
            if (data.hasOwnProperty('nexgen_name')) {
                obj['nexgen_name'] = ApiClient.convertToType(data['nexgen_name'], 'String');
            }
            if (data.hasOwnProperty('openstack_id')) {
                obj['openstack_id'] = ApiClient.convertToType(data['openstack_id'], 'String');
            }
            if (data.hasOwnProperty('openstack_name')) {
                obj['openstack_name'] = ApiClient.convertToType(data['openstack_name'], 'String');
            }
            if (data.hasOwnProperty('organizations')) {
                obj['organizations'] = ApiClient.convertToType(data['organizations'], ['Number']);
            }
            if (data.hasOwnProperty('power_usages')) {
                obj['power_usages'] = PowerUsageModel.constructFromObject(data['power_usages']);
            }
            if (data.hasOwnProperty('projects')) {
                obj['projects'] = ApiClient.convertToType(data['projects'], ['String']);
            }
            if (data.hasOwnProperty('provision_date')) {
                obj['provision_date'] = ApiClient.convertToType(data['provision_date'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('stocks')) {
                obj['stocks'] = ApiClient.convertToType(data['stocks'], [NodeStocksPayload]);
            }
            if (data.hasOwnProperty('sunset_date')) {
                obj['sunset_date'] = ApiClient.convertToType(data['sunset_date'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NodePowerUsageModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NodePowerUsageModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NodePowerUsageModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['flavors'])) {
            throw new Error("Expected the field `flavors` to be an array in the JSON data but got " + data['flavors']);
        }
        // ensure the json data is a string
        if (data['nexgen_name'] && !(typeof data['nexgen_name'] === 'string' || data['nexgen_name'] instanceof String)) {
            throw new Error("Expected the field `nexgen_name` to be a primitive type in the JSON string but got " + data['nexgen_name']);
        }
        // ensure the json data is a string
        if (data['openstack_id'] && !(typeof data['openstack_id'] === 'string' || data['openstack_id'] instanceof String)) {
            throw new Error("Expected the field `openstack_id` to be a primitive type in the JSON string but got " + data['openstack_id']);
        }
        // ensure the json data is a string
        if (data['openstack_name'] && !(typeof data['openstack_name'] === 'string' || data['openstack_name'] instanceof String)) {
            throw new Error("Expected the field `openstack_name` to be a primitive type in the JSON string but got " + data['openstack_name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['organizations'])) {
            throw new Error("Expected the field `organizations` to be an array in the JSON data but got " + data['organizations']);
        }
        // validate the optional field `power_usages`
        if (data['power_usages']) { // data not null
          PowerUsageModel.validateJSON(data['power_usages']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['projects'])) {
            throw new Error("Expected the field `projects` to be an array in the JSON data but got " + data['projects']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        if (data['stocks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['stocks'])) {
                throw new Error("Expected the field `stocks` to be an array in the JSON data but got " + data['stocks']);
            }
            // validate the optional field `stocks` (array)
            for (const item of data['stocks']) {
                NodeStocksPayload.validateJSON(item);
            };
        }

        return true;
    }


}

NodePowerUsageModel.RequiredProperties = ["openstack_id"];

/**
 * @member {Array.<String>} flavors
 */
NodePowerUsageModel.prototype['flavors'] = undefined;

/**
 * @member {String} nexgen_name
 */
NodePowerUsageModel.prototype['nexgen_name'] = undefined;

/**
 * @member {String} openstack_id
 */
NodePowerUsageModel.prototype['openstack_id'] = undefined;

/**
 * @member {String} openstack_name
 */
NodePowerUsageModel.prototype['openstack_name'] = undefined;

/**
 * @member {Array.<Number>} organizations
 */
NodePowerUsageModel.prototype['organizations'] = undefined;

/**
 * @member {module:model/PowerUsageModel} power_usages
 */
NodePowerUsageModel.prototype['power_usages'] = undefined;

/**
 * @member {Array.<String>} projects
 */
NodePowerUsageModel.prototype['projects'] = undefined;

/**
 * @member {Date} provision_date
 */
NodePowerUsageModel.prototype['provision_date'] = undefined;

/**
 * @member {String} status
 */
NodePowerUsageModel.prototype['status'] = undefined;

/**
 * @member {Array.<module:model/NodeStocksPayload>} stocks
 */
NodePowerUsageModel.prototype['stocks'] = undefined;

/**
 * @member {Date} sunset_date
 */
NodePowerUsageModel.prototype['sunset_date'] = undefined;






export default NodePowerUsageModel;

